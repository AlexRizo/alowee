---

---

<section
  class="bg-[url('img/servicios-integrales/experiencia-bg.webp')] h-screen relative section-4 snap-start"
>
  <article
    class="text-center flex flex-col items-center justify-center gap-5 h-full"
  >
    <picture class="experiencia relative top-16">
      <img
        src="img/servicios-integrales/experiencia.webp"
        alt="Como lo hacemos"
      />
    </picture>
    <h2
      class="text-7xl font-bold text-transparent bg-clip-text bg-gradient-to-b from-gray-800 to-black py-2.5"
    >
      Experiencia
    </h2>
    <p class="text-gray-800 text-xl">
      Alowee nace en 2008. A lo largo del camino hemos colaborado<br /> con tiendas
      departamentales, gobiernos, universidades,<br /> colegios, empresas de servicios,
      del sector inmobiliario,<br />
      restaurantes y firmas de valores en México y el extranjero.<br /><br /> Las
      diversas necesidades y retos de nuestros clientes nos han<br /> impulsado a
      crecer profesionalmente, elevando nuestra calidad<br /> técnica y ampliando
      nuestra oferta de servicios.
    </p>
  </article>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  const section4 = document.querySelector(".section-4") as HTMLElement;
  const children = section4.children[0].children;

  gsap.registerPlugin(ScrollTrigger);

  const defaults = {
    delay: 0.2,
    duration: 0.8,
    stagger: 0.1,
    ease: "power4.out",
  };

  ScrollTrigger.create({
    scroller: "main",
    trigger: section4,
    onEnter: () => {
      gsap.fromTo(
        children,
        {
          y: 1000,
          ...defaults,
        },
        {
          y: 0,
          ...defaults,
        }
      );
    },
  });
</script>
