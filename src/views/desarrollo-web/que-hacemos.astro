---

---

<section
  class="h-screen snap-start section-02 bg-[url('/img/servicios-integrales/experiencia-bg.webp')] bg-cover bg-center"
>
  <article class="container mx-auto flex flex-col items-center justify-center h-full text-black lg:gap-10 gap-6">
    <h1 class="2xl:text-7xl xl:text-7xl lg:text-6xl text-4xl font-bold title-s-02">¿Qué hacemos?</h1>
    <p class="text-center lg:text-xl text-lg description-p01">
      Desde sitios informativos hasta aplicaciones web interactivas,
      implementamos<br class="lg:block hidden"/> tecnologías de vanguardia para potenciar tu presencia
      digital.
    </p>
    <picture class="img-codigo lg:w-auto w-9/10">
      <img src="/img/desarrollo-web/code.webp" alt="Código" />
    </picture>
  </article>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  ScrollTrigger.create({
    scroller: 'main',
    trigger: '.section-02',
    onEnter: () => {
      gsap.fromTo(['.title-s-02', '.description-p01', '.img-codigo'], {
        y: 800,
      }, {
        y: 0,
        delay: 0.3,
        duration: 0.8,
        stagger: 0.05,
        ease: "power4.out",
      })
    }
  })
  
  
</script>