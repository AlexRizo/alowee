---
---

<section class="bg-warning-600 h-[100dvh] w-full flex items-center justify-center absolute overflow-hidden">
  <div class="lg:w-[60%] w-1/2 lg:h-full h-1/2 absolute circle-container">
    <span class="xl:size-[60vw] lg:size-[50vw] size-[90vw] bg-info-600 rounded-full absolute lg:blur-[120px] blur-[80px] circle translate-x-1/2 z-2"></span>
    <span class="xl:size-[60vw] lg:size-[50vw] size-[90vw] bg-neutral-600 rounded-full absolute lg:blur-[120px] blur-[80px] circle -translate-x-1/2 z-1"></span>
    <span class="xl:size-[40vw] lg:size-[30vw] size-[50vw] bg-warning-600 rounded-full absolute lg:blur-[120px] blur-[60px] circle -translate-x-1/4 z-3"></span>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  const timeline = gsap.timeline({ repeat: -1, yoyo: true });

  // 1. Seleccionamos los <span> hijos del <section> que contiene la clase "circle"
  const circles = document.querySelectorAll('.circle') as NodeListOf<HTMLSpanElement>;
  const circleContainer = document.querySelector('.circle-container') as HTMLDivElement;
  const circle1 = circles[0], circle2 = circles[1], circle3 = circles[2];

  timeline.to(circle1, {
    x: -100,
    y: 300,
    duration: 6,
    ease: 'power1.inOut',
  }).to(circle2, {
    x: 250,
    y: -350,
    duration: 6,
    ease: 'power1.inOut',
  }, 0).to(circle3, {
    x: 500,
    y: 200,
    duration: 6,
    ease: 'power1.inOut',
  }, 0).to(circle1, {
    x: 100,
    y: -300,
    duration: 6,
    ease: 'power1.inOut',
  }, 6).to(circle2, {
    x: -250,
    y: 300,
    duration: 6,
    ease: 'power1.inOut',
  }, 6).to(circle3, {
    x: -500,
    y: -200,
    duration: 6,
    ease: 'power1.inOut',
  }, 6);

  // Añadir rotación infinita al contenedor de círculos (animación de fondo)
  gsap.to(circleContainer, {
    rotation: 360,
    duration: 40,
    ease: 'linear',
    repeat: -1,
    transformOrigin: 'center center'
  });
</script>